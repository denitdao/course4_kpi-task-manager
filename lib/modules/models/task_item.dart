class TaskItem {
  bool isCompleted;
  String title;
  String description;
  String date;
  String subjectTitle;

  TaskItem(this.isCompleted, this.title, this.description, this.date,
      this.subjectTitle);
}

class Task {
  bool isCompleted;
  String title;
  String description;
  String date;
  String subjectTitle;

  Task(this.isCompleted, this.title, this.description, this.date,
      this.subjectTitle);
}
